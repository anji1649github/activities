pipeline{
    agent{label 'node'}
    stages{
        stage('clone'){
            steps {
            git url:'https://github.com/anji1649github/activities.git',
            branch: 'master'
            }
        }
        stage('ansible playbook'){
            steps{
            sh'ansible-playbook -i hosts apache.yml'
        }
    }
    }
}